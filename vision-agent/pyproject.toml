[project]
name = "vision-agent"
version = "0.1.0"
description = "Voice-only AI language teacher for Stream audio lessons"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "vision-agents[gemini,getstream]>=0.5.9",
]
